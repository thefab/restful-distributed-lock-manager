apiVersion: v1
kind: Service
metadata:
  labels:
    app: restful-distributed-lock-manager
    group: com.stakater.platform
    version: 0.5.1
    provider: stakater
  name: app
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8888
  selector:
    app: restful-distributed-lock-manager
    group: com.stakater.platform
    provider: stakater